specVersion: "v1"
serviceId: "alltask"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "alltask-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
